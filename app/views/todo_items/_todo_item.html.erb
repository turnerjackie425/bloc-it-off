<%= form_for [@todo_item] do |f| %>
   <%= form_group_tag(todo_item[:content]) do %>
     <%= f.text_field :content, class: 'form-control', placeholder: "Edit To-Do Item" %>
   <% end %>
     <%= f.submit "Save", class: 'btn btn-success' %>
   </div>
 <% end %>